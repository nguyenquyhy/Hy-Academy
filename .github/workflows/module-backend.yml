name: Deploy backend

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      resourceGroupName:
        required: true
        type: string
      containerAppName:
        required: true
        type: string
      containerEnvironmentName:
        required: true
        type: string
      logAnalyticsSKU:
        required: true
        type: string
      dockerTag:
        required: true
        type: string
    secrets:
      deploymentSpClientId:
        required: true
      deploymentSpTenantId:
        required: true
      deploymentSpSubscriptionId:
        required: true
    outputs:
      apiDomain:
        description: "Domain of the created API endpoint"
        value: ${{ jobs.deploy_container_app.outputs.apiDomain }}

jobs:
  deploy_container_app:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    permissions: 
      id-token: write
      contents: read
    outputs:
      apiDomain: ${{ steps.deploy_backend.outputs.applicationURL }}
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
      
    - name: Azure login
      uses: azure/login@v1.4.2
      with:
        client-id: ${{ secrets.deploymentSpClientId }}
        tenant-id: ${{ secrets.deploymentSpTenantId }}
        subscription-id: ${{ secrets.deploymentSpSubscriptionId }}
                
    - name: Deploy container app
      id: deploy_backend
      uses: azure/arm-deploy@v1.0.6
      with:
        resourceGroupName: ${{ inputs.resourceGroupName }}
        template: ./.azure/backend.bicep
        parameters: appName="${{ inputs.containerAppName }}"
          environmentName="${{ inputs.containerEnvironmentName }}"
          dockerImage="docker.io/${{ inputs.dockerTag }}"
          logAnalyticsSKU="${{ inputs.logAnalyticsSKU }}"
        failOnStdErr: false # Temporarily ignore error now until ARM specs adds Container App

    - name: Azure logout
      run: |
            az logout
      if: always()