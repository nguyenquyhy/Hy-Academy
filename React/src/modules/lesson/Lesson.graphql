query GetLesson($courseId: UUID!, $lessonId: UUID!) {
    lesson(courseId: $courseId, lessonId: $lessonId) {
        id
        title
        description
        course {
            id
            title
        }
    }
}